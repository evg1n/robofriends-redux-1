version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "doubletrouble"
workflows:
  version: 2
  bobby_sally:
    jobs:
      - bobby
      - sally